{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "stm32-load",
            "type": "shell",
            "command": "${workspaceRoot}/STM32-files/scripts/stm32-load",
            "args": [
                "${command:cmake.launchTargetPath}",
            ]
        }
    ]
}